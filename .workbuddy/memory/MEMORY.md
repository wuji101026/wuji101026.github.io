# 项目长期记忆 · wuji101026.github.io

## 音乐功能（已删除 · 2026-08-15）
- 用户于 2026-08-15 17:42 要求「删除音乐功能」。
- 已移除 `index.html` 末尾的背景音乐 `<script>` 块（自动播放 + 响度平衡 + 首次交互 fallback 整套逻辑）。
- 已删除 `music/` 目录（含 `bgm.mp3`）。
- 历史脚本 `build-music-manifest.js`、`build-manifest.html`、`manifest.json` 仍保留在 `_obsolete/`（用户此前要求"移入 _obsolete 而非删除、可恢复"），未动。
- 结论：当前站点**无任何音乐功能、无音频资产、无相关 UI**。

## 用户沟通偏好
- 中文环境；干练直接，少铺垫，直接给结论/可执行结果。
- 需求描述偏口语且 terse，常含看似矛盾的表述，落地前需先对齐"谁在操作"（站长 vs 访客）。
